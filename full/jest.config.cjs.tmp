module.exports = {
    testEnvironment: 'node', // Use the Node.js environment for tests
    // extensionsToTreatAsEsm: ['.js'], // Treat `.js` files as ESM
    transform: {}, // Disable Babel transforms if not using them
};
